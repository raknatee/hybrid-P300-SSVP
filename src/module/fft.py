
# def to_fft(data:list[float],sampling_rate:float):
     
#     df = sampling_rate/len(len(data))
#     m = ssvp.eeg.mean(axis=1)
#     X = np.arange(0,230/2,df)
#     X = X[X<20]
#     fft = np.fft.fft(m)